APP_PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=carniceria
DB_PASSWORD=secret
DB_NAME=carniceria_db
DB_ROOT_PASSWORD=rootsecret

# Path to static files (auto-overridden in Docker)
PUBLIC_DIR=../public

# JWT (JSON Web Token) — autenticación
JWT_SECRET=genera-aqui-64-caracteres-aleatorios
JWT_EXPIRES_IN=8h
BCRYPT_ROUNDS=12
